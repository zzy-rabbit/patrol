package internal

import (
	"github.com/gofiber/fiber/v2"
)

func (s *service) registerRouter(fiberApp *fiber.App) {
	fileGroup := fiberApp.Group("/patrol/api/v1/file")
	fileGroup.Post("/commit", s.CommitFile)
	fileGroup.Delete("", s.DeleteFile)
}
